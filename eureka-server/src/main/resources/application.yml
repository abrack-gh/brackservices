spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      refresh:
        enabled: true
      probability: 1.0
server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

